#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()
{
    int n, i;

    printf ("Numero:");
    scanf ("%d",&n);

    for (i = 1; i < n+1; i++){
        if (n%i==0)
        {
            printf ("%d\n", i);
        }
    }

    return 0;
}
